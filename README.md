# 5000 — GeoField monorepo (root)

Asosiy ilova katalogi: [`geofield_pro_flutter/`](geofield_pro_flutter/README.md).

- **Flutter** loyiha — yuqoridagi katalogda.
- **CI** — [.github/workflows/ci.yml](.github/workflows/ci.yml).
- **Reliz APK/AAB** gitga commit qilinmasin; artefaktlar CI yoki mahalliy `build/` dan olinadi.

Qisqa xavfsizlik eslatmasi: `android/key.properties`, keystore (`.jks`) va `google-services.json` maxfiylik siyosati bo‘yicha boshqariladi; ularni ochiq repoda ulash mumkin emas.
